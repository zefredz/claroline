
> > > > > > > > > > > CLAROLINE 1.5 RC1 - BUG LIST < < < < < < < < < < <


1. DOCUMENT. Quand on crée un document avec l'outil create document, si on ne met pas 
de nom pour le fichier il y a une erreur et donc fichier pas enregistré, 
mais si on fait back le formulaire ne se rerempli pas donc on pert tout 
ce qu'on a fait.

Repéré par : SEB
Assigné à : HUGUES
Etat      : RESOLU

2. DOCUMENT. Pour les liens créés dans document, on pensait qu'ajouter l'url en 
<a href... sur la page .url ne serait pas une mauvaise chose pour prévenir 
les cas ou la redirection ne se passe pas bien.

Repéré par : SEB
Assigné à : HUGUES
Etat      : RESOLU


3.  INSTALL instal repetées = plusieurs admins -> utilisation des function d'ajout utilisateurs
Assigné à : 
Etat : à résoudre pour : 1.5.1

4. HTMLAREA. Bug signalé par PRFB sur le forum.  Insertions de <br /> aux retours à la ligne dans le code html des pages créées

5. COURSE_HOME. librairie à déplacer ? course_home.lib.php se trouve dans le répertoire même de course_home

6. TOOL LIST && COURSE_HOME. Les URL me semblent mal formatées, malgré le fait qu'elles fonctionnent. Exemple : http://localhost/~pir/clarocvs/claroline15rc1/claroline/../claroline/learnPath/learningPathList.php .  La valeur du path de l'outil ne devrait pas contenir de ../ dans la mesure on on essaie de plus en plus d'utiliser des url absolues à partir des $*Sys et $*Web du fichier de config

7. DOCUMENT.  upload d'une page HTML contenant des images, problèmes lors de la modifications des paths des images au sein du document, les images ne s'affichent pas du tout.

8. CLARO_MAIN.CONF. présence de code couleurs qui ne sont, à ma connaissance pas utilisés.  Ils sont signalé comme étant utilisés dans le banner mais ne le sont pas.

9. CLARO_INIT_BANNER. Il reste plusieurs tag <font> dans le code qu'il serait intéressant de remplacer par une meilleure utilisation des css. Il reste également des codes couleurs hardcodés pour les différents <td>

Assigné à : HUGUES

10. claroline/admin/adminUserCourseSettings.php -> on ne sais pas changer le champ role

11. claro_disp_button()- Bug Avec Mozilla - Quand il y a htmlArea dans la même page, le 'onclick' 
sur le <button> du claroButton est cpaturé par htmlArea. Résultat c'est le <form> dans lequel est htmlArea qui est envoyé et pas la commande claroButton.

Repéré par : HUGUES


-------------
$Log$
Revision 1.12  2004/06/11 12:27:57  peeters
no message

Revision 1.11  2004/06/11 12:13:38  peeters
no message

Revision 1.10  2004/06/11 12:11:41  peeters
no message

Revision 1.9  2004/06/11 11:57:50  peeters
no message

Revision 1.8  2004/06/11 11:57:33  peeters
no message

Revision 1.7  2004/06/11 11:54:24  peeters
no message

Revision 1.6  2004/06/11 10:34:06  moosh
- bug 10

Revision 1.5  2004/06/11 10:09:25  seb
new : bug 4 to 9

Revision 1.4  2004/06/11 10:04:22  moosh
- bug 3 : install
